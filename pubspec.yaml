name: base_flutter
description: flutter base for initation flutter project with startup project needs

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get_it: ^7.2.0
  injectable: ^1.5.3
  go_router: ^2.5.3
  flutter_bloc: ^8.0.0
  flutter_localizations:
    sdk: flutter
  intl: ^0.17.0
  dio: ^4.0.4
  logger: ^1.1.0
  sprintf: ^6.0.0
  equatable: ^2.0.3
  dartz: ^0.10.1
  cached_network_image: ^3.2.0
  photo_view: ^0.13.0  #why using library for this simple action
  like_button: ^2.0.4   #why using library for this simple action
  font_awesome_flutter: ^9.2.0
  rxdart: ^0.27.3



dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  # add the generator to your dev_dependencies
  injectable_generator:
  # add build runner if not already added
  #command for run injectable package --> flutter packages pub run build_runner build
  build_runner:




flutter:
#  generate: true
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
flutter_intl:
  enabled: true
